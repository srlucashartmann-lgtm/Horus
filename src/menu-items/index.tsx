import { warRoom, quantitativo, qualitativo, inteligencia, configuracoes } from './horus';
import { NavItemType } from 'types/menu';

const menuItems: { items: NavItemType[] } = {
  items: [warRoom, quantitativo, qualitativo, inteligencia, configuracoes]
};

export default menuItems;
